extends layout

block content

  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='name') Category:
      input#name(type='text', placeholder='Breakfast, Snacks etc.' name='name' required='true' value=(undefined===category ? '' : category.name) )
      label(for='description') Description:
      input#description(type='text', placeholder='Short description...' name='description' value=(undefined===category ? '' : category.description) )
    button#submit(type='submit') Submit

  if errors
    ul
      for error in errors
        li!= error.msg
