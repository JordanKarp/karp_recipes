extends layout

block content
  h1= title

  ul(class='bulletedList')
    each person in user_list
      li
        a(href=person.url) #{person.username || person.name}
    else
      li There are no users.
