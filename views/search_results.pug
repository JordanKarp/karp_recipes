extends layout

block content
  h1= title

  h4 Search for: #{search_term} (#{results.length})
  ul
    each thing in results
      li
        a(href=thing.url) #{thing.title}

    else
      li No Results.