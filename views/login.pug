extends layout

block content
  h1 Please login below:

  - console.log(req)


  form(method='POST' action='' name='loginForm')
    label(for='username' hidden) Username:
    input#username(type='text' placeholder='Username' name='username' required='true' value=(undefined===user ? '' : user.username) )
    label(for='password' hidden) Password:
    input#password(type='password' placeholder='Password' name='password' required='true' value=(undefined===user ? '' : user.password))
    button#submit(type='submit' action='/catalog') Login
    
  
  p Or 
    a(href='/register') click here
    |  to create an account